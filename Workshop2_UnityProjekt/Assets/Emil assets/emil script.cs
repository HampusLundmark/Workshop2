using UnityEngine;

public class emil_script : MonoBehaviour
{
    public float coolhet = 10.7f;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
